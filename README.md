# Projeto_Final_Erick_Avelino_Lemos_Tecnicas_de_Desenvolvimento_de_Algoritmos
🚗 Sistema CRUD de Gerenciamento para Concessionária (PHP &amp; MySQL) Este projeto é uma aplicação web básica desenvolvida em PHP puro, utilizando MySQL para a persistência de dados. O objetivo é fornecer uma interface de gerenciamento (CRUD) para as principais entidades de uma concessionária de veículos.

✨ Funcionalidades PrincipaisO sistema permite a gestão completa (Cadastro, Edição, Listagem e Exclusão - CRUD) das seguintes entidades:EntidadeArquivos de Cadastro/EdiçãoDescriçãoClientescadastrar-cliente.php, editar-cliente.phpGerenciamento de informações de clientes (nome, CPF, contato, endereço, etc.).Funcionárioscadastrar-funcionario.php, editar-funcionario.phpGerenciamento da equipe de vendas e suporte.Marcascadastrar-marca.php, editar-marca.phpCadastro das fabricantes de veículos (Honda, Ford, etc.).Modelos (Veículos)cadastrar-modelo.php, editar-modelo.phpCadastro de veículos, associando-os a uma Marca existente (chave estrangeira).Vendascadastrar-venda.phpRegistro de transações, vinculando Cliente, Funcionário e Modelo (chaves estrangeiras).

⚙️ Estrutura e TecnologiaO projeto segue uma arquitetura simples baseada em arquivos e utiliza:Linguagem: PHP (Puro)Banco de Dados: MySQL/MariaDBConexão: API MySQLi do PHPArquivos ChaveArquivoFunçãoconfig.phpDefine constantes de conexão e estabelece o objeto de conexão $conn com o banco de dados. Essencial para o funcionamento do sistema.cadastrar-*.phpContém os formulários HTML para a criação de novos registros, enviando os dados para a ação salvar-*.editar-*.phpRealiza uma consulta SELECT inicial para buscar e pré-preencher os dados do registro, permitindo a modificação (UPDATE).cadastrar-venda.phpContém lógica de consulta (SELECT) para popular campos de seleção (<select>) com dados de Cliente, Funcionário e Modelo.salvar-*.php (Implícitos)Arquivos que recebem a submissão dos formulários e executam comandos INSERT ou UPDATE no banco de dados, baseados no valor do campo oculto acao.

🛠️ Como Instalar e RodarPara executar este projeto localmente, siga os passos abaixo:Pré-requisitosVocê precisará de um ambiente de servidor local, como XAMPP, WAMP ou MAMP, com PHP e MySQL configurados.Clone o repositório:Bashgit clone
cd concessionaria-crud

Configurar o Banco de Dados:Acesse o PHPMyAdmin (ou ferramenta de gestão MySQL de sua preferência).Crie um banco de dados com o nome definido no config.php: concessionaria2122n.Crie as tabelas necessárias (cliente, funcionarios, marca, modelo, venda). (Recomenda-se criar um arquivo database.sql com o esquema das tabelas para facilitar a importação).Ajustar a Conexão:Edite o arquivo config.php e verifique se as constantes HOST, USER, PASS e BASE correspondem à sua configuração local do MySQL.Acessar a Aplicação:Coloque a pasta do projeto no diretório raiz do seu servidor local (ex: htdocs no XAMPP).Acesse a aplicação no navegador (http://localhost/concessionaria-crud/index.php).

✍️ ContribuiçãoEste projeto está aberto a melhorias e sugestões. Sinta-se à vontade para abrir uma issue ou enviar um Pull Request!
